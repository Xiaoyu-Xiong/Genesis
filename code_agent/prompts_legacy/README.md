Legacy snapshot of the pre-SimDebug prompt files.

The active prompts live in `code_agent/prompts/`. Keep this directory as an
observation/reference copy while prompt-derived simulation debugging guidance
moves into `code_agent/context/simdebug/`.
